{
    "$schema": "http://json-schema.org/schema",
    "$id": "BuilderAdd",
    "title": "Builder Add",
    "type": "object",
    "properties": {
        "skipInstall": {
            "type": "boolean",
            "description": "Skip the installation of the dependencies",
            "default": false
        }
    }
}