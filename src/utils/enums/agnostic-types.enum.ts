export enum AgnosticFieldType {
  STRING = 'string',
  NUMBER = 'number',
  BOOLEAN = 'boolean',
  DATE = 'date',
  OBJECT_ID = 'ObjectId',
  MAP = 'map',
  ARRAY = 'array',
  UNKNOWN = 'unknown',
}
